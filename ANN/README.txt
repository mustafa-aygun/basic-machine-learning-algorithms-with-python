The explanation about the code pieces commented on the source code file. To run it on local, it is necessary that having anaconda setup with pytorch. If you have pytorch on local machine, you can create a folder where you gonna locate source file.Then basically open the folder in visual studio code and run the code. It will download CIFAR-10 on the local folder. Also, the program saving best models to that local folder and during the testing get models from there. Moreover, to run it from command line, you can use Python ANN.py command too.